# 电力市场每日动态 — 项目参考手册

> 本文档整合了项目所有关键信息，开启新对话时将此文档内容提供给 AI 即可快速恢复上下文。

---

## 一、项目概览

| 项目 | 详情 |
|------|------|
| **名称** | 电力市场每日动态 |
| **在线地址** | `https://action2212.github.io/electricity-market-daily/` |
| **GitHub 仓库** | `action2212/electricity-market-daily` |
| **本地路径** | `C:\Users\24675\WorkBuddy\2026-07-24-14-53-31\electricity-market-daily\` |
| **托管方式** | GitHub Pages（静态站点） |
| **参考原型** | `https://zhangye01.github.io/energy-daily/` |
| **说明** | 每日自动搜集电力市场最新政策、交易规则、市场动态、储能发展和行业事件，生成 Markdown 日报并通过 GitHub Pages 发布 |

---

## 二、文件结构

```
electricity-market-daily/
├── index.html              # 前端页面（纯 HTML+CSS+JS，marked.js 渲染 Markdown）
├── .gitignore              # 仅忽略 .ghtoken
├── reports/                # 日报 Markdown 文件
│   ├── manifest.json       # 报告索引（数组，最新在前）
│   ├── 电力市场每日动态_2026-07-24.md
│   ├── 电力市场每日动态_2026-07-25.md
│   ├── 电力市场每日动态_2026-07-26.md
│   ├── 电力市场每日动态_2026-07-27.md
│   ├── 电力市场每日动态_2026-07-28.md
│   └── 电力市场每日动态_2026-07-29.md
└── 项目参考手册.md          # 本文件
```

---

## 三、GitHub 仓库信息

- **仓库名**：`action2212/electricity-market-daily`
- **Token**：`<GITHUB_TOKEN>`（实际 token 见本地 `.ghtoken` 文件或 WorkBuddy 会话记录）
- **GitHub Pages**：已启用，分支 `main`，根目录 `/`
- **推送方式**：GitHub Contents API（Python 脚本）
  - Git 命令行推送在端口 443 被阻断，必须使用 REST API 上传文件
  - 本地 `.git` 仓库仍然维护，用于版本追踪
  - 获取文件 content 的 base64：`curl -H "Authorization: token <TOKEN>" "https://api.github.com/repos/action2212/electricity-market-daily/contents/reports/文件名.md"`
  - 更新文件：`PUT` 到 `https://api.github.com/repos/action2212/electricity-market-daily/contents/文件路径`，body 包含 `message`、`content`（base64）、`sha`（从 GET 获取）

---

## 四、自动化配置

| 字段 | 值 |
|------|-----|
| **ID** | `automation-1784882117310` |
| **名称** | 电力市场每日动态 |
| **调度** | 每天 8:00 AM（RFC 5545 RRULE: `FREQ=DAILY;BYHOUR=8;BYMINUTE=0`）|
| **工作目录** | `C:\Users\24675\WorkBuddy\2026-07-24-14-53-31\electricity-market-daily` |
| **状态** | ACTIVE |

---

## 五、信息搜集策略

### 优先级

**第一优先（官方来源）**：
- 国家能源局官网 `www.nea.gov.cn`
- 国家发改委官网及各省发改委网站
- 国网新能源云公众号
- 各省电力交易中心（广东、山东、山西、浙江、江苏、四川、蒙西等）

**第二优先（行业媒体）**：
- 储能与电力市场公众号
- 北极星电力市场网公众号

**补充来源**（WebSearch 关键词）：
- 电力中长期交易、现货市场规则、辅助服务、储能政策、虚拟电厂、电力市场化交易、绿电交易、容量市场

### 搜集流程
1. 逐个搜索第一优先级来源最新发布
2. 搜索第二优先级来源
3. 用关键词补全
4. 每类来源至少 3-5 条
5. 去重：同一新闻多来源标注全部来源

---

## 六、报告格式规范

### 文件名
`电力市场每日动态_YYYY-MM-DD.md`（如 `电力市场每日动态_2026-07-29.md`）

### 报告结构

```markdown
# 电力市场每日动态（XXXX年X月X日）

## 今日要闻
> 今日收录 **N** 条动态，按热度排序
（4-6条重要动态，含选题方向）

## 政策法规
（按热度排序）

## 交易规则
（按热度排序）

## 市场动态
（按热度排序）

## 储能发展
（按热度排序）

## 行业事件
（按热度排序）

## 本期数据亮点
| 指标 | 数据 | 变化 | 来源 |
|------|------|------|------|

## 本周重点政策跟踪
| 政策文件 | 状态 | 影响领域 | 生效时间 |
|----------|------|----------|----------|

*简报由 WorkBuddy 自动生成 | 数据来源：公开信息 | 热度标签综合来源优先级与搜索热度 | XXXX年X月X日*
```

### 单条新闻格式

```markdown
- <span class="hot-badge hot-3">热门</span> **标题** — 内容摘要（来源：XXX，X月X日）

  > 🎯 **选题方向**：《文章标题》
  > **切入角度**：xxx
  > **框架**：①②③④⑤
  > **读者**：xx
```

### 热度标签系统

| 标签 | HTML class | 含义 |
|------|-----------|------|
| 热门 | `hot-badge hot-3` | 国家级政策/重大市场事件/第一优先级重要新闻 |
| 关注 | `hot-badge hot-2` | 省级重要政策/行业重大事件/多来源转载 |
| 一般 | `hot-badge hot-1` | 常规动态/企业公告/小事件 |

### 选题方向规则（仅热门 hot-3）

- 仅 `hot-3`（热门）条目生成选题，选题紧跟在热门条目下方（blockquote）
- 热门条目与 blockquote 之间**必须有一行空行**
- **不生成独立的"公众号选题方向"章节**
- 格式包含 4 个字段：
  - 🎯 **选题方向**：《文章标题》— 有吸引力，书名号包裹
  - **切入角度**：1-2句话，具体维度/数据/对比
  - **框架**：①②③④⑤ 4-5个部分
  - **读者**：2-4类目标受众，顿号分隔
- **不包含热度预估/评分**

---

## 七、前端架构

- **技术栈**：纯 HTML + CSS + JS
- **Markdown 渲染**：`marked.js` CDN (`https://cdn.jsdelivr.net/npm/marked/marked.min.js`)
- **布局**：左侧边栏（280px 深绿色 `#1a3a2a`）+ 右侧内容区
- **功能**：
  - 日期列表（从 manifest.json 加载）
  - 分类筛选按钮（全部/政策/交易/市场/储能/事件）
  - 关键词搜索
  - 热门标签 CSS：红/橙/灰色圆形徽标
  - 选题 blockquote 样式：绿色左边框 + 浅绿背景
- **配色**：侧边栏 `#1a3a2a`、内容区白色卡面、绿色强调色

---

## 八、manifest.json 格式

```json
[
  "电力市场每日动态_2026-07-29.md",
  "电力市场每日动态_2026-07-28.md",
  "..."
]
```

数组按日期降序排列，最新在前。新增报告时**插入到数组开头**。

---

## 九、推送更新流程

由于 Git 命令行在端口 443 被阻断，使用 GitHub Contents API：

```python
import requests, base64

TOKEN = "<YOUR_GITHUB_TOKEN>"
REPO = "action2212/electricity-market-daily"
HEADERS = {"Authorization": f"token {TOKEN}", "Accept": "application/vnd.github.v3+json"}

def upload_file(path, content):
    url = f"https://api.github.com/repos/{REPO}/contents/{path}"
    # 1. GET 获取 sha（如果文件已存在）
    r = requests.get(url, headers=HEADERS)
    sha = r.json().get("sha") if r.status_code == 200 else None
    # 2. PUT 上传
    body = {"message": f"Update {path}", "content": base64.b64encode(content.encode()).decode()}
    if sha: body["sha"] = sha
    requests.put(url, headers=HEADERS, json=body)
```

更新后约 1-2 分钟 GitHub Pages 自动部署。

---

## 十、已知问题与解决方案

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| `git push` 超时（443） | 网络环境阻断 Git 协议 | 使用 GitHub Contents API 上传 |
| 选题与热点条目匹配失败 | 中文文本微小差异导致正则匹配不上 | 使用多关键词子串匹配 + 字符重叠度加分 |
| 文件内容被脚本覆盖损坏 | 批量编辑脚本正则不精确 | 用 `git show HEAD:path > file` 恢复后重新处理 |

---

## 十一、后续使用指南

### 在新对话中恢复项目

将以下关键信息提供给 AI：

1. 本文件（`项目参考手册.md`）的全部内容
2. 当前日期和待生成报告的日期
3. 项目本地路径

### 常见操作

**生成新一天的报告**：
> "请按照项目参考手册中的格式，为 2026年7月30日 生成电力市场每日动态报告，搜集最新信息并更新 manifest.json，通过 GitHub API 上传。"

**补充历史报告**：
> "请为 2026年7月XX日 补充生成电力市场每日动态报告。"

**修改已有报告**：
> "请修改 `reports/电力市场每日动态_2026-07-29.md`，在 XX 分类下新增一条关于 YY 的新闻。"

**更新前端样式**：
> "请修改 `index.html` 中的 XX 样式。"

### 关键约束提醒

- 文件名日期格式：`YYYY-MM-DD`
- 每条新闻 2 句话以内
- 来源标注可溯源
- 热度标签使用 HTML span
- 仅 hot-3 条目带选题方向
- 选题方向内联在对应条目下方，无独立章节
- manifest.json 保持正确 JSON 且最新在上
- PDF 或 Word 导出见下方补充说明

---

## 十二、补充：多格式导出

如需将日报导出为 PDF 或 Word，可以使用以下方式：

### PDF 导出
```bash
# 使用浏览器打印：在页面加载后 Ctrl+P → 另存为 PDF
# 或使用 Puppeteer/Playwright 自动截图导出
```

### Word 导出
```bash
# 使用 pandoc
pandoc reports/电力市场每日动态_2026-07-29.md -o output.docx
```

---

*最后更新：2026年7月29日*
